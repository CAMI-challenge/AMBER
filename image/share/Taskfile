default: evaluate.py -g ${LABELS} -f ${FASTA} ${PREDICTIONS} -o ${OUTPUT} > ${OUTPUT_FILE}
precision_recall_per_genome: precision_recall_per_genome.py -g ${LABELS} -f ${FASTA} ${PREDICTIONS} > ${OUTPUT_FILE}
